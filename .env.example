# Market data provider
DATA_API_KEY=your_data_api_key_here

# Paper mode is the default in this MVP.
# Broker credentials are placeholders for future integrations and are unused by default.
BROKER_API_KEY=your_broker_api_key_here
BROKER_API_SECRET=your_broker_api_secret_here

# Deployment/runtime
APP_ENV=development
SERVICE_NAME=theta
HOST=0.0.0.0
PORT=8000
DATABASE_URL=sqlite+pysqlite:///data/theta.db
STRICT_ENV_VALIDATION=false
RUN_MIGRATIONS_ON_STARTUP=true
LIVE_TRADING=false
# Comma-separated list. For Render static frontend use:
# CORS_ALLOWED_ORIGINS=https://thetadata.onrender.com
CORS_ALLOWED_ORIGINS=http://localhost:3000
LOG_DIR=logs
CACHE_DIR=data/cache

# Optional runtime overrides
INITIAL_CAPITAL=100000
POSITION_SIZE_PCT=1.0
FIXED_FEE=1.0
SLIPPAGE_PCT=0.0005
# Leave blank to disable stop loss / take profit.
STOP_LOSS_PCT=
TAKE_PROFIT_PCT=
MAX_POSITION_SIZE=1.0
MAX_DAILY_LOSS=2000
PAPER_TRADING=false
MAX_NOTIONAL_PER_TRADE=100000
MAX_OPEN_POSITIONS=10
EXECUTOR_DAILY_LOSS_CAP=2000
TRADE_LOG_PATH=logs/trades.csv

# Worker controls (paper-only)
WORKER_NAME=default-worker
WORKER_ENABLE_TRADING=false
WORKER_POLL_SECONDS=60
WORKER_SYMBOL=SPY
WORKER_TIMEFRAME=1d
WORKER_STRATEGY=moving_average_crossover
WORKER_STRATEGY_PARAMS_JSON={}
WORKER_ORDER_QUANTITY=1.0
WORKER_FORCE_REFRESH=false

# Additional risk + runtime guards
TRADING_START=09:30
TRADING_END=16:00
ALLOW_AFTER_HOURS=false
KILL_SWITCH_ON_STARTUP=false
